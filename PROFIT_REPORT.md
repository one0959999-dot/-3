# 수익 최대화 종합 리포트 (H) — 야간 자율작업 결과

생성: 2026-06-24 야간 / 목표: "이 프로그램으로 수익을 최대로 낼 수 있는 조건인지" 검증

## 1. 데이터 기반 (single source of truth)
- 백테스트 신호 총 **2,098,669건** (KR 608,592 / US 1,490,092)
- KR: pykrx 정수가격, 2014+, 소수점오염 0 (yfinance back-adjust 오염 제거 완료)
- US: S&P500 + 나스닥 보통주 (ETF 제외)
- 섹터캐시 3,252건, 상폐 8건(부실5/피인수3, 자동분류)

## 2. 검증 완료 (A~G + 추가 구조점검)
| 항목 | 결과 |
|---|---|
| A. US 앙상블 게이트 | KR과 통일 ✅ |
| B. 통합 연동 | 8/8 통과 ✅ |
| C. 거래비용 | 라이브·백테스트 정상(백테스트가 보수적) ✅ |
| E. 앙상블 균형 | 0건 아님, 선별적(고품질) ✅ |
| G. 코드정리 | 신호함수 중복 0, base.signals 단일기준 6파일 공유 ✅ |
| forecast 인덱스 | idx_bts_mode_phase 사용(풀스캔X) ✅ |
| forecast fallback | 표본부족→phase-avg 폴백 정상 ✅ |
| KR AI 후보선정 | screener→ai_select_satellites+exp_return 연결 ✅ |
| US 예측블록 | KR과 통일(매수시 목표/보유/손절 표시) ✅ |
| delisting 분류 | 주가경로+DART, ≥20행 가드, 8건 정확 ✅ |

## 3. 핵심 데이터 인사이트 (수익 최대화 근거)
- **국면별 승률차 큼**: PANIC 86%/73%(10%/20%달성) >> BULL_LATE 65%/41%
  → 공포장(PANIC/BEAR_MID) 매수가 통계적 최고. 후행상승장(BULL_LATE) 보수적 권장.
- **앙상블(신호+점수) 최고**: +4.5% vs 점수제 +3.8% vs 엔진 +2.3% (대결 검증)

## 4. 수익최적화 4종 — **전부 구현 완료** (2026-06-24 야간)
1. **killswitch 2단계** ✅ (f8e5646): 일중 고점대비 -10%→신규매수중단, -20%→전량청산+당일거래중단. KR+US, `KILLSWITCH_ENABLED`로 토글.
2. **F. 진입필터 win20** ✅ (87d4d9f): win10(변별력없음)+win20(20%달성률) 둘 다 게이트. BULL_LATE(win20 41%) 자연배제, PANIC(74%) 통과. 점수도 win10+win20로 고품질 국면 우선.
3. **D. 청산 표시=실제** ✅ (4e0d1e7): 매수시 백테스트 통계(손절=avg max_drawdown, 익절=avg max_gain)를 포지션에 저장→관리루프에서 강제. 메시지에 띄운 손절/목표가 실제로 동작. KR+US.
4. **US AI후보선정 통일** ✅ (1823663): US도 exp_return(백테스트 예상수익) 산출+정렬. AI종합선정은 `ai_select_us_satellites`로 이미 존재(audit 정정).

## 5. 미해결 (외부요인)
- **상폐 대량수집(1500~2000)**: KRX get_market_ticker_list IP차단 지속(밤새 0) → 쿨다운 풀리면 자동수집. 인플로우 분류는 작동 중(8건).

## 결론
구조·연동·비용·데이터활용 **모두 건전**, 앙상블로 점수제보다 +0.7%p 우위.
수익최적화 4종(killswitch·win20필터·청산정렬·US통일) **전부 구현 완료** → 실전 투입 준비됨.

## 실전 사용 주의 (남은 환경 리스크)
- **PC 절전으로 봇 중단**(야간 2회 발생): 실거래 중 봇이 꺼지면 손절 불가 → 절전 끄기 필수.
- killswitch는 일중 고점기준 — 첫 가동시 `KILLSWITCH_ENABLED=True` 확인.
- 처음엔 소액/모의 권장(백테스트 검증과 라이브 체결은 다를 수 있음).
- 상폐 대량수집(1500~2000)은 KRX 차단 해제 후 `python tools/add_delisted_kr.py`.
